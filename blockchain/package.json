{
  "name": "blockchain-audit",
  "version": "1.0.0",
  "description": "Immutable audit ledger smart contract",
  "scripts": {
    "compile": "hardhat compile",
    "deploy": "hardhat run scripts/deploy.js --network local"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^3.0.0",
    "hardhat": "^2.20.0"
  }
}
